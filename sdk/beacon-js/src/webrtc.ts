/**
 * WebRTC instrumentation: polls RTCPeerConnection.getStats() through the AMS
 * WebRTC adapter, derives startup time, freeze/rebuffer detection, bitrate and
 * resolution switches, and connection errors.
 */

// TODO(SDK-01, Phase 2)
export {};
