# Shared components

Cross-feature UI primitives only (charts, time-range picker, data table, stat
card, empty/loading states). Feature-specific components stay in their feature
folder; promotion to here requires a second consumer.

Owner: FE-01.
