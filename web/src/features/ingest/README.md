# Feature: Publisher & Ingest Health (F4) — Phase 2

Per-publisher bitrate, fps, keyframe interval, packet loss/jitter, source-drop
detection, stream health score (documented + reproducible). Degradation visible
within 15 seconds.

Owner: FE-01. Data: `/qoe/ingest`.
