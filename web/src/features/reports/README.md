# Feature: Usage & Billing Reports (F6) — Phase 2

Per-app/stream/tenant viewer-minutes, peak concurrency, egress GB, recording
storage; tenant-mapping configuration (stream-name pattern / metadata tag);
scheduled CSV exports; white-label PDF header (Phase 3). Egress estimation
method is always displayed on the report.

Owner: FE-01. Data: `/reports/usage`, `/reports/schedules`.
