# Feature: Settings — Phase 1

AMS source configuration (REST endpoint + credentials, log path, Kafka brokers,
webhook secret), API/ingest tokens, license key entry + tier display, local users.
This is the 15-minute-install surface (PRD §7.8) — onboarding flow quality here
is a launch acceptance criterion, not polish.

Owner: FE-01. Data: `/admin/sources`, `/admin/license`.
