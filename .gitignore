# Binaries / build output
/server/bin/
/web/dist/
/sdk/beacon-js/dist/
*.exe

# Dependencies
node_modules/
/server/vendor/

# Local env & data
.env
.env.*
!.env.example
pulse_secret.key
/deploy/data/
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite

# Docker file-based secrets (deploy/secrets/*.txt) — never commit
/deploy/secrets/*.txt

# Demo handout — contains a live admin token; never commit (share privately)
/oguz-testing.md

# Editor / OS
.DS_Store
.idea/
*.swp

# Test artifacts
coverage/
*.out
*.tsbuildinfo

# Built binaries
/server/pulse
/qa/mock-ams/mock-ams

# ClickHouse server artifacts (integration tests / local stack)
access/
metadata/
preprocessed_configs/
format_schemas/
user_files/
flags/
/data/
/store/
uuid
status

# Real-AMS recon captures — contain third-party secrets (AMS licence key, RTSP creds). Never commit.
agents/handoffs/real-ams-captures/

# Playwright local artifacts (CI uploads its own copies)
web/playwright-report/
web/test-results/

# Real-AMS validation evidence (timestamped JSON/screenshot packages — never commit)
qa/realams/evidence/

# licensegen local build artifact (CI uses `go run .`)
qa/licensegen/licensegen
