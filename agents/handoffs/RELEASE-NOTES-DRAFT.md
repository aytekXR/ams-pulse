# Release notes — SHIPPED as v0.2.0 (2026-07-09, D-066)

> This draft was executed: the notes now live on the GitHub release —
> https://github.com/aytekXR/ams-pulse/releases/tag/v0.2.0
> (`gh release view v0.2.0`). Release run 29023647495 (CI-gated, Trivy,
> multi-arch, SBOM + provenance, cosign). Prod rolled onto the tag same day.
> For the NEXT release: start from `CHANGELOG.md` `[Unreleased]` and the
> structure of the v0.2.0 notes; keep the header-trim convention (everything
> above the first `---` is instructions, everything below is the release body).
