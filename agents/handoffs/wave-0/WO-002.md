# WO-002 — Wave 0: fix beacon SDK packaging so the size gate runs (SDK-01)

**Issued by:** ORCH-00 · 2026-06-11
**Assigned:** SDK-01 (charter: `agents/definitions/SDK-01-beacon-sdk.md`)

## Objective

`npm run size` in `sdk/beacon-js` must execute the 15 KB gate successfully.
Known defect: tsup emits `dist/index.*` but `package.json` `main`/`module` and
the size-limit config reference `dist/pulse-beacon.*`, so size-limit fails with
file-not-found.

## In-scope files

`sdk/beacon-js/package.json`, `sdk/beacon-js/tsconfig.json` (only if required).

## Acceptance criteria

- `cd sdk/beacon-js && npm run build && npm run size` exits 0 and reports a
  size under 15 KB (trivially true for the stub).
- `main`, `module`, `exports` (if added) and size-limit all reference files tsup
  actually emits.
- No SDK source changes (wave 2 implements the SDK).

## Completion report

`agents/handoffs/wave-0/WO-002-report.md` with verification output.
