# WO-105 — Wave 1 verification (QA-01)

**Issued by:** ORCH-00 · 2026-06-11
**Assigned:** QA-01 (charter: `agents/definitions/QA-01-qa.md`)
**Gate:** manifest wave 1 — "compose-up to live dashboard on real AMS; alert <30s;
install <15min" — adapted per decision D-002 (no Docker): local process stack.

## Objective

Independently verify wave-1 acceptance criteria end-to-end on a local stack and
file defects. VERIFY, NEVER FIX.

## In-scope files

`qa/` (new), `.github/workflows/ams-version-matrix.yml` step content (your stub),
test fixtures. Defects → report only.

## Work items

1. **Mock AMS server** (`qa/mock-ams/`, Go preferred): emulates REST v2 surface
   amsclient uses (apps, broadcasts list/stats, WebRTC client stats, cluster
   nodes, system stats), writes a synthetic `ant-media-server-analytics.log`
   (JSON lines, AMS shapes), sends webhooks. Scriptable scenario: N streams
   publish/unpublish on a timeline, viewer counts evolve, a stream "fails".
   Make it reusable: the validation phase and the AMS version matrix will drive it.
2. **End-to-end gate run** (`qa/wave-1/`): script that (a) starts ClickHouse
   (`/tmp/clickhouse server`, temp data dir), (b) `pulse migrate` + `pulse serve`
   with a config pointing at mock AMS, (c) drives a scenario, then asserts via
   the public API: new stream visible ≤10 s (poll /live/streams, measure);
   viewer counts within ±2% of mock's truth; stream-offline alert notification
   ≤30 s (Slack channel pointed at a local HTTP sink you run; measure
   detection→delivery); rules survive `pulse` restart; /healthz reports all
   components. Record all measured numbers.
3. **Budget regression tests:** codify the measurable wave-1 rows of
   ARCHITECTURE §4 into repeatable scripts/tests under `qa/budgets/`.
4. **Install-time walkthrough:** follow README/compose instructions as written
   (dry-run analysis where Docker is required), time the local-stack path, file
   doc defects for anything missing/wrong. The <15-min criterion is verified as:
   steps complete + no undocumented manual fixes needed.
5. **Frontend smoke:** `npm run build && npm run test` in web/; load the built
   UI against the running stack (serve via pulse static hosting), verify the
   live dashboard shows the scenario streams (browser automation or DOM-level
   fetch assertions — keep it lightweight).

## Acceptance criteria

`qa/wave-1/gate-report.md` with PASS/FAIL per gate criterion, measured latencies,
repro commands, and a defect list (owner, severity, minimal repro). Exit nonzero
scripts on failure so reruns are mechanical.

## Out of scope

Fixing anything. Beacon/QoE flows (wave 2 gate).

## Completion report

The gate report IS the completion report.
