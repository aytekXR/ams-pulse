# WO-106 — Wave 1 documentation (DOC-01)

**Issued by:** ORCH-00 · 2026-06-11
**Assigned:** DOC-01 (charter: `agents/definitions/DOC-01-docs.md`)
**PRD refs:** §7.12 GTM (install guide is a launch asset)

## Objective

Docs current with wave-1 reality; the 15-minute install runbook exists and
matches what QA-01 verified.

## In-scope files

`docs/`, root `README.md`, component README content.

## Work items

1. `docs/runbooks/install.md`: the 15-minute guide — Docker Compose path (primary,
   authored from deploy/ even though unexecuted here per D-002 — label it as the
   supported path), manual/binary path (the QA-verified local-stack steps),
   first-run wizard walkthrough, troubleshooting table (from QA defects/findings).
2. Update `docs/ARCHITECTURE.md` where implementation diverged or refined the
   sketch (component diagram stays truthful; note live-aggregates design, meta
   encryption, bootstrap-token flow per BE reports).
3. Root `README.md`: real quick start, feature status table (shipped wave 1 vs
   coming), link map (architecture, runbooks, contracts, agents).
4. `docs/runbooks/alerting.md`: rule semantics, channel setup (SMTP, Slack
   webhook), test-fire, cooldown/grouping/maintenance windows, default pack.
5. New ADRs only if implementation made decisions ADR-worthy (read completion
   reports; e.g. WS protocol envelope, encryption-at-rest approach) — else skip.

## Acceptance criteria

Every documented command actually runs (test them); no documented-but-
unimplemented behavior (label roadmap items); QA-01's doc defects addressed.

## Out of scope

Code, contracts; API reference prose (generated from OpenAPI — just link it).

## Completion report

`agents/handoffs/wave-1/WO-106-report.md`.
