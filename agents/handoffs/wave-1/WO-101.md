# WO-101 — Contract freeze, full surface (INT-01)

**Issued by:** ORCH-00 · 2026-06-11
**Assigned:** INT-01 (charter: `agents/definitions/INT-01-integration.md`)
**PRD refs:** §7.9 F1–F10, §7.10 architecture, §7.11 tiers
**Decision basis:** D-004 (single full freeze covering waves 1+2+3-MVP)

## Objective

Finalize the entire contract surface so every implementation agent builds against
frozen shapes: complete OpenAPI spec, finalized event schemas, complete ClickHouse
and meta-store DDL. After this WO, contracts change only via ORCH-00-approved
change requests.

## In-scope files

Everything under `contracts/`.

## Work items

### 1. OpenAPI (`contracts/openapi/pulse-api.yaml`)

Replace every "TODO" response with real schemas in `components.schemas`. Required
surface (current 23 method+paths, plus promotions):

- **Promote to real paths** (today only YAML comments): `POST /ingest/beacon`
  (request = BeaconEvent envelope per the event schema; auth = `X-Pulse-Ingest-Token`
  header; 202 on accept, 401/413/422/429 errors), `GET /metrics` (Prometheus text
  exposition, no auth or separate scrape token — document choice), `GET /healthz`
  (component status JSON, unauthenticated).
- **Wave-3 MVP additions:** `GET /anomalies` (F9: baseline-deviation flags with
  metric, scope, observed vs expected, sigma) and `GET /probes` + `POST /probes` +
  `GET /probes/{probeId}/results` (F10: synthetic probe config + results).
- **Common machinery:** bearer-token `securitySchemes` applied to all `/api/v1`
  paths; a single `Error` envelope (code, message, details?) used by all 4xx/5xx;
  shared parameters: `from`, `to` (epoch ms or RFC3339 — pick one and document),
  `app`, `stream`, `node`, `tenant`, `interval` (hour|day), `limit`/`cursor`
  pagination on list endpoints.
- **Response schemas** must cover everything the feature READMEs under
  `web/src/features/*/README.md` enumerate (they are the FE acceptance source):
  live overview (totals, per-app, per-node health, protocol mix), live streams
  (virtualizable list w/ viewer counts, publisher state, health score), audience
  (timeseries + totals: views, uniques, watch-time, peak concurrency), geo
  (country/region rows), devices (device/os/browser/protocol rows), qoe summary
  (startup p50/p95, rebuffer ratio, error rate, bitrate timeline, sliceable),
  qoe ingest (per-publisher bitrate/fps/keyframe/packet-loss/jitter timelines +
  health score + drop events), alert rules/channels/history CRUD shapes
  (incl. maintenance windows, cooldown, grouping key, channel test-fire result),
  usage reports (per app/stream/tenant: viewer-minutes, peak concurrency, egress
  GB with method field, recording GB) + report schedules CRUD, fleet nodes
  (role, status, last_seen, version, per-node load), admin sources CRUD
  (REST url, credential env ref, log path, kafka brokers, webhook secret —
  secrets write-only, never echoed), license info (tier, limits, validity),
  ingest tokens CRUD (`/admin/tokens`), WS message envelope for `/live/ws`
  (typed: snapshot | delta | heartbeat) documented via the path description.
- Document in the spec description that live endpoints serve from in-memory
  aggregates (not ClickHouse) — fixes mapped gap #4.

### 2. Event schemas (`contracts/events/`)

- `ams-server-event.schema.json`: finalize `data` per event type (oneOf keyed on
  `type`): stream_publish_start/end (stream name, app, publish type), stream_stats
  (viewer counts by protocol, bitrate, speed), webrtc_client_stats (per-client rtt,
  jitter, packet loss), ingest_stats (bitrate, fps, keyframe interval, packet loss,
  jitter), node_stats (cpu, mem, disk, net in/out, jvm), recording_ready (path,
  size, duration), viewer_join/leave (**add** `viewer_id`, `protocol`, and
  connection metadata — fixes gap #5). Add optional ingest-enrichment block
  (`geo.country`, `geo.region`, `client.device`, `client.os`, `client.browser`)
  documented as collector-added, not AMS-sent — fixes gaps #2/#3.
- `beacon-event.schema.json`: finalize per-type `data` (startup_complete: startup_ms;
  rebuffer_end: duration_ms; error: code, message?, fatal; bitrate_change: from,
  to; resolution_change: from, to; heartbeat: watch_ms, bitrate, dropped_frames?).
  CMCD-aligned names where applicable (PRD F3). Same enrichment-block note.
- `alert-notification.schema.json`: align `scope` with server-event field naming
  (gap #6); add `cooldown_until`, `group_key`, `test` (bool, for test-fires).

### 3. ClickHouse DDL (`contracts/db/clickhouse/0001_init.sql`)

Complete DDL: `server_events`, `beacon_events` (both MergeTree, partitioned by
date, ORDER BY tuned for stream/time queries, TTL 90 days — retention configurable
at runtime via `pulse migrate` variable substitution: document mechanism),
`viewer_sessions` (ReplacingMergeTree keyed on session_id),
`rollup_audience_1h/_1d`, `rollup_qoe_1h/_1d`, `rollup_usage_1d` (SummingMergeTree
or AggregatingMergeTree — justify in comments; TTL 13 months) **including geo
(country, region) and device (device, os, browser, protocol) dimensions** (gap #9),
plus materialized views populating the rollups from raw tables. Low-footprint
defaults per ARCHITECTURE §3.6 (2-vCPU tier).

### 4. Meta DDL (`contracts/db/meta/0001_init.sql`)

Complete DDL (SQLite/Postgres common subset; document divergences as comments):
`users` (bcrypt hash, role), `api_tokens` (hash, scopes, expiry), `ingest_tokens`,
`ams_sources` (encrypted credential field), `cluster_nodes`, `alert_rules` (metric,
operator, threshold, window_s, scope, severity, cooldown_s, group_by, muted,
maintenance windows as JSON), `alert_channels` (type, encrypted config JSON),
`alert_history`, `report_schedules` (cron, scope, tenant mapping, format, white-label
header JSON), `tenants` (name, stream pattern / metadata tag mapping — F6),
`license` (key, tier, signature, offline file path), `probes` + `probe_results`
pointer table if meta-appropriate (results may belong in ClickHouse — decide,
document), `anomaly_baselines` (metric, scope, window stats) if meta-appropriate
(same call), `schema_migrations`.

### 5. Codegen wiring

Add `web` codegen instructions: a documented command (openapi-typescript) in
`contracts/README.md` + npm script name FE-01 should add. Provide JSON-Schema test
fixtures dir `contracts/events/fixtures/` with ≥2 valid + ≥1 invalid example per
schema (used by server and SDK round-trip tests).

## Acceptance criteria

- `npx @redocly/cli lint contracts/openapi/pulse-api.yaml` — zero errors.
- `npx ajv-cli compile --spec=draft2020 -s <each schema>` — compiles; fixtures
  validate/fail as labeled (`ajv validate`).
- ClickHouse DDL executes cleanly: `/tmp/clickhouse server` (start in background
  with a temp data dir) then feed the migration through `clickhouse-client`
  (or `/tmp/clickhouse client`); all tables + MVs create.
- Meta DDL executes cleanly in SQLite (`sqlite3 :memory:` or python3 sqlite3).
- Zero remaining `TODO` markers on shapes in `contracts/`.
- No AMS-specific naming leaks into beacon/rollup/api shapes (portability rule).

## Out of scope

Any implementation code; web/, server/, sdk/ files.

## Completion report

`agents/handoffs/wave-1/WO-101-report.md`: contract changelog (every shape added/
changed), verification output, decisions made on open questions (timestamps format,
probe-results placement, anomaly-baseline placement, /metrics auth).
