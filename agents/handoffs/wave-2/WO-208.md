# WO-208 — Wave 2 documentation (DOC-01)

**Issued by:** ORCH-00 · 2026-06-12
**Assigned:** DOC-01 (charter: `agents/definitions/DOC-01-docs.md`)
**PRD refs:** §7.12 GTM items 2 (open-source SDK) and 3 (SEO guides end in install)

## Objective

Docs current with wave-2 reality: SDK integration guide (launch asset),
QoE/reports/fleet feature docs, Prometheus/Grafana guide, Helm install path.

## In-scope files

`docs/`, root `README.md`.

## Work items

1. `docs/guides/beacon-sdk.md`: integration walkthrough per player (AMS JS SDK,
   hls.js, video.js, plain video element) from SDK-01's README + verified
   against the real built SDK; token setup via the settings UI; sampling and
   privacy/anonymize-IP guidance.
2. `docs/guides/prometheus.md`: scrape config example, metric reference table
   (from BE-02's exposition), Grafana starter panel JSON (hand-authored,
   small) — the F8 "convert the DIY-Grafana crowd" asset.
3. `docs/runbooks/install.md`: add the Helm path (authored-unexecuted label per
   D-002, link INFRA-01's chart README); update env var table for wave-2 vars
   (ingest listener, mmdb path, metrics token, S3); mmdb acquisition note
   (D-007.4: user-supplied GeoLite2, registration link, path config).
4. `docs/runbooks/reports.md`: tenant mapping semantics (pattern/tag,
   precedence, unassigned), egress estimation method disclosure, schedule
   setup, white-label config, reconciliation (`pulse diag --reconcile`).
5. Update `docs/runbooks/alerting.md`: new channels (Telegram, PagerDuty,
   webhook + HMAC verification snippet), new rule types, cron maintenance
   windows.
6. `docs/ARCHITECTURE.md` + root `README.md`: feature status table flips F2
   (full), F3, F4, F6, F7, F8 to shipped; component diagram gains ingest
   listener + scheduler; health-score formula reference (BE-01's doc).
7. New ADRs only where wave-2 made ADR-worthy decisions (read completion
   reports; candidates: beacon ingest hardening posture, egress estimation
   model, Kafka client choice) — else skip.

## Acceptance criteria

Every documented command/config runs against the wave-2 build (test them);
QA-01's wave-2 doc defects addressed; no documented-but-unimplemented behavior
(label Phase-3 roadmap items).

## Out of scope

Code, contracts, marketing prose beyond the guides above.

## Commit (decision D-008)

When (and only when) your acceptance criteria pass, commit your changes
yourself: stage ONLY explicit paths inside your scope (never `git add -A`,
`-u` or `.` — other agents share this working tree), commit message
`<YOUR-AGENT-ID> <THIS-WO-ID>: <summary>` with verification evidence in the
body, do NOT push. If `.git/index.lock` is busy, wait briefly and retry —
never delete the lock. Partial or blocked work is reported, not committed.

## Completion report

`agents/handoffs/wave-2/WO-208-report.md`.
