# WO-206 — Wave 2 infrastructure: Helm, compose updates, CI hardening (INFRA-01)

**Issued by:** ORCH-00 · 2026-06-12
**Assigned:** INFRA-01 (charter: `agents/definitions/INFRA-01-infra.md`)
**PRD refs:** §7.10 deployment model (Helm in Phase 2), §7.13 risks (version matrix)
**Constraint:** D-002 — no Docker/cluster here: author + lint-validate only,
label execution status honestly.

## Objective

Helm chart authored and lint-clean, compose/deploy artifacts current with the
wave-2 binary surface, CI jobs covering the new components.

## In-scope files

`deploy/`, `.github/`, `Makefile`, `.gitignore`.

## Work items

1. **Helm chart** (`deploy/helm/pulse/`): chart for collector+UI binary,
   ClickHouse (subchart or bundled StatefulSet — justify; default single
   replica, low-footprint tuning from ARCHITECTURE §3.6), optional Postgres
   for meta (values switch vs SQLite PVC); ingest listener as separate Service
   (the only internet-facing one — annotate); values for every PULSE_* env the
   binary reads (cross-check `server/cmd/pulse/config.go` and
   `deploy/config/pulse.example.yaml`); resource requests/limits per 2-vCPU
   Free-tier sizing; README with install/upgrade/values table.
   Validation: `helm lint` + `helm template` rendering golden-file test in CI
   (no cluster — D-002; record limitation).
2. **Compose updates** (`deploy/`): new env vars from waves 1–2 (ingest
   listener, metrics token, mmdb path volume mount, S3 export creds via env),
   beacon ingest port exposed; pin image digests; healthchecks for all
   services.
3. **CI** (`.github/workflows/ci.yml`): add sdk job real steps if SDK-01's
   lint/test scripts landed (build+size+lint+test); add `helm lint`/template
   job; ensure web job runs generate:api before build (types drift guard);
   Go job runs with CGO_ENABLED=0 and includes `-tags integration` step gated
   behind a label or nightly (ClickHouse download in CI — document cost).
4. **AMS version matrix** (`.github/workflows/ams-version-matrix.yml`): make
   the workflow runnable against real AMS images in CI (matrix: the AMS
   versions QA-01 names in qa/), wiring QA's mock/gate scripts where real AMS
   is unavailable; coordinate via QA's D-W1-006 test names — workflow file is
   yours, test content is QA's.
5. **Makefile:** targets for helm-lint, mock-ams build, local-stack up/down
   (wrapping qa/ scripts for the developer path documented in runbooks).

## Acceptance criteria

- `helm lint` zero failures; `helm template` renders with default values and
  with postgres+s3 values (commit golden outputs under deploy/helm/tests/).
- `docker compose config` validates (config-level check runs without the
  daemon) for the updated compose file.
- `make help` lists new targets; every target runs or fails with a clear
  no-Docker message (D-002 honesty rule).
- CI YAML passes actionlint (npx or binary).

## Out of scope

Application code, contracts, docs prose (DOC-01 — but your Helm README is
yours).

## Commit (decision D-008)

When (and only when) your acceptance criteria pass, commit your changes
yourself: stage ONLY explicit paths inside your scope (never `git add -A`,
`-u` or `.` — other agents share this working tree), commit message
`<YOUR-AGENT-ID> <THIS-WO-ID>: <summary>` with verification evidence in the
body, do NOT push. If `.git/index.lock` is busy, wait briefly and retry —
never delete the lock. Partial or blocked work is reported, not committed.

## Completion report

`agents/handoffs/wave-2/WO-206-report.md`: chart design decisions, values
table summary, CI changes, what could NOT be executed here (D-002 list),
gaps/CRs.
