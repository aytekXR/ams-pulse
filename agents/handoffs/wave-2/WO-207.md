# WO-207 — Wave 2 verification (QA-01)

**Issued by:** ORCH-00 · 2026-06-12
**Assigned:** QA-01 (charter: `agents/definitions/QA-01-qa.md`)
**Gate (manifest wave 2):** "beacon→dashboard round trip; billing reconciles ±1%"
— plus the wave-2 budget rows of ARCHITECTURE §4. Adapted per D-002/D-007.5.

## Objective

Independently verify wave-2 acceptance end-to-end on the local stack. VERIFY,
NEVER FIX.

## In-scope files

`qa/` (extend mock-ams, new `qa/wave-2/`, extend `qa/budgets/`),
version-matrix test content coordinated with INFRA-01's workflow.

## Work items

1. **Mock AMS extensions** (`qa/mock-ams/`): per-publisher WebRTC client stats
   with scriptable degradation (bitrate drop, fps collapse, packet loss);
   analytics-log keyframe/bitrate events; cluster node add/remove on the
   scenario timeline; recording_ready events; delivered-bytes where the real
   AMS would emit them (document which AMS surface you emulate).
2. **Beacon round-trip gate** (`qa/wave-2/`): drive the REAL SDK (build
   sdk/beacon-js, run in node/jsdom or a tiny headless harness) against
   `pulse` ingest with a valid token: events accepted (202), visible in QoE
   API ≤ the SDK's 10 s batching + collector flush (measure end-to-end);
   tampered token → 401; malformed event → 422. Sampling honored.
3. **Billing reconciliation gate:** scripted scenario with known truth
   (N streams, fixed viewer counts, fixed bitrates, known durations, two
   tenants by pattern) → run a statement → assert every figure within ±1% of
   computed truth; measure generation time (<60 s budget); verify
   `pulse diag --reconcile` agrees.
4. **Budget regressions** (extend `qa/budgets/`): ingest degradation visible
   ≤15 s (script mock encoder drop, poll ingest API); node autodiscovery
   ≤2 min (scenario adds node); 13-month rollup query <3 s (seed 13 months of
   synthetic rollups directly into ClickHouse, time the API); /metrics scrape
   parses + stays bounded-cardinality under 500-stream scenario; SDK size
   gate re-run.
5. **Tier-gate verification:** Free vs Pro vs Business behavior at the API
   level per the WO-203 matrix (use dev license keys from BE-02's report).
6. **Regression sweep:** wave-1 gate script still green (`qa/wave-1/run-gate.sh`);
   full build/lint/test across server, web, sdk.
7. **AMS version matrix (D-W1-006):** write the `TestAMSVersionMatrix`
   integration test content against mock-ams emulation profiles (per-version
   shape differences you emulate); document which assertions need real AMS
   containers (CI-only, INFRA-01's workflow runs them).

## Acceptance criteria

`qa/wave-2/gate-report.md`: PASS/FAIL per criterion with measured numbers,
repro commands, defect list (owner, severity, minimal repro), waived items
limited to D-002/D-007.5 class. Scripts exit nonzero on failure.

## Out of scope

Fixing anything. Anomaly/probe flows (wave 3 gate).

## Commit (decision D-008)

When (and only when) your acceptance criteria pass, commit your changes
yourself: stage ONLY explicit paths inside your scope (never `git add -A`,
`-u` or `.` — other agents share this working tree), commit message
`<YOUR-AGENT-ID> <THIS-WO-ID>: <summary>` with verification evidence in the
body, do NOT push. If `.git/index.lock` is busy, wait briefly and retry —
never delete the lock. Partial or blocked work is reported, not committed.

## Completion report

The gate report IS the completion report.
