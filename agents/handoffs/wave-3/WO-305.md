# WO-305 — Wave 3-MVP documentation (DOC-01)

**Issued by:** ORCH-00 · 2026-06-14
**Assigned:** DOC-01 (charter: `agents/definitions/DOC-01-docs.md`)
**PRD refs:** F9, F10

## Objective

Docs current with Wave 3-MVP: anomaly detection and synthetic probes documented and
verified against the built binary; feature status flipped.

## In-scope files

`docs/`, root `README.md`.

## Work items

1. `docs/guides/anomaly-detection.md` (or a runbook): what F9 flags, the statistical
   model (rolling baselines, sigma sensitivity, min-sample gate), default
   sensitivity and how to tune it, the <1 false-alarm/node-week intent, and how
   flags surface (`GET /anomalies` + the UI). From BE-02's WO-302 report, verified.
2. `docs/runbooks/probes.md`: creating a synthetic probe (URL/protocol/interval),
   what is measured (success/TTFB/bitrate), protocol coverage + documented limits
   (HLS full; webrtc/rtmp/dash minimal per WO-301), where results appear, and the
   "synthetic vs organic" labeling. Verified against a running probe.
3. `docs/ARCHITECTURE.md` + `README.md`: flip F9 + F10 to shipped (MVP) with the
   measured QA numbers; component diagram gains the probe runner + anomaly tick;
   note the minimal-but-working scope (D-001) and Phase-3 deltas (mobile beacons,
   SSO, white-label PDF, distributed probe network, multi-node edge dedup).
4. New ADRs only if Wave 3-MVP made ADR-worthy calls (anomaly model choice, probe
   protocol coverage) — read the completion reports; else skip.

## Acceptance criteria

Every documented command/config runs against the wave-3 build (test them); QA-01's
wave-3 doc defects addressed; no documented-but-unimplemented behavior (label any
Phase-3 roadmap items clearly).

## Out of scope

Code, contracts, marketing prose.

## Commit (D-008)

Verify, then commit — explicit `docs/...`/`README.md` paths only (never `git add
-A`/`-u`/`.`), `DOC-01 WO-305: <summary>` + evidence, no push. Index lock busy ⇒
wait+retry, never delete.

## Completion report

`agents/handoffs/wave-3/WO-305-report.md`.
