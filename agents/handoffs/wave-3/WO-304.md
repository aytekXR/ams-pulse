# WO-304 — Wave 3-MVP verification (QA-01)

**Issued by:** ORCH-00 · 2026-06-14
**Assigned:** QA-01 (charter: `agents/definitions/QA-01-qa.md`)
**Gate:** F9 anomaly detection meaningful at default sensitivity (<1 false alarm/
node-week target); F10 probe round-trip (config → runner → `probe_results` → API/UI,
clearly labeled). Adapted per D-002/D-007.5.

## Objective

Independently verify Wave 3-MVP (F9 + F10) end-to-end on the local stack. VERIFY,
NEVER FIX.

## In-scope files

`qa/` (extend mock-ams / fixtures, new `qa/wave-3/`, extend `qa/budgets/`).

## Work items

1. **Probe round-trip gate** (`qa/wave-3/`): stand up an `httptest`/local HLS origin
   (healthy + degradable). Create a probe via `POST /probes` pointing at it; let the
   runner execute (short interval); assert a `probe_results` row lands in ClickHouse
   and is returned by `GET /probes/{id}/results` with measured TTFB/bitrate/success;
   degrade the origin → next result is a failure with the right error_code; confirm
   the UI/API labels results as synthetic. Measure config→first-result latency.
2. **Anomaly false-alarm gate:** drive a steady synthetic metric stream (known mean
   + noise) through the baseline updater at default sensitivity for a simulated
   node-week (fast-clock or batched); assert the flag rate maps to **<1 false alarm/
   node-week**. Then inject a genuine sustained deviation and assert it IS flagged
   (true-positive). Report both numbers and the sensitivity config used.
3. **Tier-gate verification:** `/anomalies` Enterprise-gated, `/probes` Pro+-gated
   per WO-302's matrix (dev license keys from BE-02's report).
4. **Regression sweep:** wave-1 + wave-2 gates still green (`qa/wave-1/run-gate.sh`,
   `qa/wave-2/run-gate.sh`, `qa/budgets/run-budget-tests.sh`); full build/lint/test
   across server, web, sdk; SDK size gate.
5. **Conformance:** kin-openapi over `/anomalies` + `/probes` endpoints.

## Acceptance criteria

`qa/wave-3/gate-report.md`: PASS/FAIL per criterion with measured numbers, repro
commands, defect list (owner, severity, minimal repro); waivers limited to
D-002/D-007.5 class. Scripts exit nonzero on failure.

## Out of scope

Fixing anything. The F1–F10 validation sweep + deferred tenant CRUD (next phase).

## Commit (D-008)

Verify, then commit — explicit `qa/...` paths only (never `git add -A`/`-u`/`.`),
`QA-01 WO-304: <summary>` + evidence, no push. Index lock busy ⇒ wait+retry, never
delete. The gate report IS the completion report.
