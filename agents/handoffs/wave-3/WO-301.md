# WO-301 — Wave 3-MVP data plane: F10 probe runner + probe_results store (BE-01)

**Issued by:** ORCH-00 · 2026-06-14
**Assigned:** BE-01 (charter: `agents/definitions/BE-01-backend-dataplane.md`)
**PRD refs:** F10 (§7 F10 — synthetic viewer probes); acceptance "probe results visible alongside organic QoE with clear labeling"
**Contracts (frozen):** `openapi/pulse-api.yaml` (Probe, ProbeResult, `/probes/{id}/results`), `db/clickhouse/0001_init.sql` (`probe_results`), `db/meta/0001_init.sql` (`probes`)
**Prereq reading:** wave-2 `WO-202-report.md` (your EventSink/store patterns), the INT-01 design notes at the top of `contracts/db/clickhouse/0001_init.sql` (probe_results placement ruling), `docs/ARCHITECTURE.md` §3.

## Objective

The single synthetic probe runner (D-001 minimal-but-working): periodically plays
configured streams from outside the server, measures real playback success/latency,
and writes time-series results to ClickHouse — the data half of F10.

## In-scope files

`server/internal/store/clickhouse/`, `server/internal/domain/` (interface only —
shared with BE-02, this is the F10 seam), a new `server/internal/prober/` (runner),
`server/cmd/pulse/` (runner wiring only, declare edits), `server/go.mod`, `go.sum`.

## Work items

1. **`probe_results` ClickHouse store** (`store/clickhouse`): writer
   (`InsertProbeResult`) + time-range reader (`QueryProbeResults(probeID, from, to,
   limit)`) matching the frozen `probe_results` DDL (id, probe_id, ts DateTime64(3),
   success, ttfb_ms, error_code, error_msg, bitrate_kbps). Reader returns rows for
   BE-02's `GET /probes/{id}/results`.
2. **ProbeConfigSource seam** (`internal/domain`): define the interface the runner
   depends on (BE-02 implements it over meta in WO-302 — the EventSink pattern):
   ```go
   type ProbeConfig struct { ID, Name, URL, Protocol string; IntervalS, TimeoutS int; Enabled bool }
   type ProbeResult struct { ID, ProbeID string; TS time.Time; Success bool; TTFBMs uint32; ErrorCode, ErrorMsg string; BitrateKbps float32 }
   type ProbeConfigSource interface {
       ListEnabled(ctx context.Context) ([]ProbeConfig, error)
       RecordResult(ctx context.Context, r ProbeResult) error  // updates probes.last_* denorm fields
   }
   ```
   Document these exact signatures in your report — BE-02 builds against them.
3. **Probe runner** (`internal/prober`): a scheduler loop that, per enabled probe,
   every `interval_s`, runs a playback check with `timeout_s`:
   - **HLS (minimum, must work):** GET the manifest, then fetch the first media
     segment; measure TTFB (ms to first byte), success (2xx + parseable), and an
     estimated `bitrate_kbps` (segment bytes / segment duration). Honest error_code
     (timeout, dns, http_4xx/5xx, parse) + error_msg on failure.
   - **webrtc/rtmp/dash:** minimal-but-honest — either a basic reachability check or
     a documented "not-yet-probed" result with a clear error_code; do NOT fake a
     success. Note the limitation in your report (Phase-3 full coverage).
   - On each result: `store.InsertProbeResult(...)` AND `source.RecordResult(...)`.
   - Concurrency-bounded (small worker pool); jittered scheduling; clean shutdown.
4. **Wiring (D-005):** construct the runner in `pulse serve` behind the
   ProbeConfigSource BE-02 provides (leave a clearly-marked hook if BE-02's impl is
   not yet present, same as wave-1 assembly hooks). Declare every cmd edit.

## Acceptance criteria (run them; report measured numbers)

- `CGO_ENABLED=0 go build ./... && go vet ./... && go test ./...` green.
- Runner unit test against an `httptest` HLS origin: a healthy manifest+segment →
  success result with TTFB+bitrate measured; a 500/timeout origin → failure result
  with correct error_code; result is written to a fake ProbeConfigSource +
  store. Use fake clock for the interval loop.
- ClickHouse integration test (build tag `integration`, `/tmp/clickhouse`): insert
  N probe_results, `QueryProbeResults` returns them time-ordered within range.
- Interval honored: a probe with `interval_s` runs ~once/interval under fake clock
  (measure, report).

## Out of scope

Probe CRUD API, the meta `probes` store impl, `GET /probes/{id}/results` handler,
anomaly detection (all BE-02, WO-302); web/; contracts edits (CRs via ORCH-00).

## Commit (D-008)

Verify acceptance, then commit yourself — stage ONLY explicit paths in your scope
(never `git add -A`/`-u`/`.`; D-011 incident — list each path), message
`BE-01 WO-301: <summary>` + evidence, no push. `.git/index.lock` busy ⇒ wait+retry,
never delete. Partial/blocked work is reported, not committed.

## Completion report

`agents/handoffs/wave-3/WO-301-report.md`: exact ProbeConfigSource/ProbeResult
signatures for BE-02, runner design + protocol coverage matrix, measured numbers,
deps added, cmd edits declared, gaps/CRs.
