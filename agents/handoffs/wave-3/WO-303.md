# WO-303 — Wave 3-MVP frontend: anomalies + probes surfaces (FE-01)

**Issued by:** ORCH-00 · 2026-06-14
**Assigned:** FE-01 (charter: `agents/definitions/FE-01-frontend.md`)
**PRD refs:** F9 (anomaly flags surfaced), F10 (probe results **alongside organic QoE with clear labeling** — the acceptance criterion)
**Contracts (frozen):** `openapi/pulse-api.yaml` — GENERATED types only (regenerate first).
**Prereq reading:** your wave-2 `WO-205-report.md`; the BE reports `WO-301-report.md`/`WO-302-report.md` for API shapes.

## Objective

The dashboard surfaces for F9 anomaly flags and F10 synthetic probes — minimal,
honest, gate-aware — completing the F1–F10 UI.

## In-scope files

`web/` (everything).

## Work items

1. **Regenerate types** (`npm run generate:api`) — generated types ONLY (git-grep
   proof in report; freeze stands, no new spec but keep local types current).
2. **Anomalies (F9, `/anomalies`):** flag list/table — metric, scope, observed vs
   expected (baseline mean), sigma (z-score), detection time; severity-ish styling
   by sigma magnitude; empty state ("no anomalies — baselines still learning" when
   sample counts are low). Optional sigma-sensitivity selector if the API supports
   the query param. Gate-aware: Enterprise-tier upsell when not entitled (per
   WO-302 matrix) — never a broken page.
3. **Probes (F10, `/probes`):** CRUD UI — list (name, url, protocol, interval,
   enabled, last result success/TTFB), create/edit form (validate interval ≥ 30,
   protocol enum, URL), delete with confirm; per-probe results view (recent
   `probe_results`: success/TTFB/bitrate/error timeline). **Synthetic labeling:**
   probe results must be CLEARLY marked synthetic and, where shown near QoE,
   visually distinct from organic beacon data (the PRD F10 acceptance) — a labeled
   "Synthetic probes" section/badge, not mixed silently into organic charts. Gate
   to Pro+ with upsell when not entitled.
4. **Loading/error/empty states** on both surfaces via the shared components.
5. **Nav:** add `/anomalies` and `/probes` routes/links consistent with the
   existing RR7 structure.

## Acceptance criteria

- `npm run build && npm run lint && npm run test` green; generated types only
  (git-grep proof in report).
- Component tests: anomaly flag rendering (observed/expected/sigma), probe form
  validation (interval<30 rejected), probe results rendering, tier-gated views
  (entitled vs not), synthetic-labeling present.
- Screenshots of both surfaces against seeded fixtures in the report.

## Out of scope

Server code, contracts edits (CRs), the deferred F6 tenant CRUD (validation sweep).

## Commit (D-008)

Verify, then commit — explicit `web/...` paths only (never `git add -A`/`-u`/`.`;
D-011 — list each path), `FE-01 WO-303: <summary>` + evidence, no push. Index lock
busy ⇒ wait+retry, never delete. Partial work reported, not committed.

## Completion report

`agents/handoffs/wave-3/WO-303-report.md`: surfaces built, screenshots, library
decisions, gaps/CRs.
